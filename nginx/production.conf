# ================================================================
# GSIS — nginx/production.conf
# Configuração de produção (Etapa 2)
# ================================================================
#
# Configuração base para produção com HTTPS.
# Adaptar com o domínio real e certificado SSL antes de usar.
#
# server {
#     listen 80;
#     server_name gsis.oriximina.pa.gov.br;
#
#     # Redireciona HTTP para HTTPS
#     return 301 https://$host$request_uri;
# }
#
# server {
#     listen 443 ssl http2;
#     server_name gsis.oriximina.pa.gov.br;
#
#     ssl_certificate     /etc/letsencrypt/live/gsis.oriximina.pa.gov.br/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/gsis.oriximina.pa.gov.br/privkey.pem;
#     ssl_protocols       TLSv1.2 TLSv1.3;
#
#     root /usr/share/nginx/html;
#     index login.html;
#
#     location / {
#         try_files $uri $uri/ /login.html;
#         gzip on;
#         gzip_types text/css application/javascript text/html;
#         add_header Cache-Control "public, max-age=3600";
#     }
#
#     location /api/ {
#         proxy_pass         http://backend:3001;
#         proxy_http_version 1.1;
#         proxy_set_header   Host              $host;
#         proxy_set_header   X-Real-IP         $remote_addr;
#         proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
#         proxy_set_header   X-Forwarded-Proto https;
#         proxy_read_timeout 60s;
#     }
#
#     access_log /var/log/nginx/GSIS_access.log;
#     error_log  /var/log/nginx/GSIS_error.log;
# }

